from django.contrib import admin

# Keep this file intentionally minimal — user management is performed
# via the custom UI; the admin can be used by superusers when needed.

# If you want to register additional helpers for the users app, add them here.